A experimental repository, contains a dynamically-loadable `main` function (which is hard to explain what it is for), which is written in Rust and behaves exactly the same to the one written in C.

```bash
./start.sh
```
